# reports

Versioned validation evidence from campaign runs.

| File | Purpose |
|---|---|
| `gmat-batch-60s.md` / `gmat-batch-60s.json` | Compatibility campaign: 12 satellite × template combinations, pass/fail per stage. |
| `gmat-batch-work/` | Working artefacts of campaign runs (log, intermediate). |
