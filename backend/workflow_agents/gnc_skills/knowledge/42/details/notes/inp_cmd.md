# Inp_Cmd 细节说明

## 产品级结论

`Inp_Cmd.txt` 不是所有案例都必须用。  
在当前自定义 `CFS_FSW` 架构下，模式切换通常应优先由内部状态机控制，而不是依赖脚本按时间切换。

## 适合使用 `Inp_Cmd.txt` 的场景

- 需要明确按时间触发的外部事件
- 需要演示脚本、Banner、GUI 视角序列
- 需要内置 FSW 的指令驱动

## 不适合作为主模式机来源的场景

- 需要按姿态误差、角速度、轨道状态、传感器有效性切换模式

这类应优先落到：

- `AcMode.c`
- `AcStateMachine.c`
