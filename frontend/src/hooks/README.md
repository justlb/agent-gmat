# frontend/src/hooks

Custom React hooks.

| File | Purpose |
|---|---|
| `useTaskStream.ts` | Streams SSE task events from the backend into a React state. |
| `useBomInfo.ts` | Reads BOM data from the current workspace. |
| `useWorkspaceAppState.ts` | Reads current workspace identity and status. |
